# TODO

## 双击托盘弹出信息面板

**状态**：未实现，等有时间再做。tooltip 那版（三行：功耗 / 每 1% 时长 / 预计剩余）已经上了，
覆盖了「随手看一眼」的需求；这个面板要解决的是 tooltip 装不下的部分。

### 为什么还要一个面板

tooltip 只有 128 个宽字符、只能显示纯文本、而且**只在鼠标悬停的一瞬间存在**。以下三样它装不下：

1. **最近实测的每格时长** —— 真实走完的 `79→78%` 用了多久。比 `Rate` 的瞬时外推可信得多：
   瞬时功率会因为一次编译、一个视频剧烈抖动，实测值是那一整格的平均。
2. **电池健康** —— 循环次数、设计容量 vs 当前满充容量、出厂日期。这些是「看一次就够」的静态信息，
   塞进每次悬停都要读的 tooltip 里是干扰。
3. **停留观察** —— 想盯着功耗调设置（比如对比开不开某个软件差多少瓦），需要一个不会自己消失、
   且每秒刷新的窗口。

### 面板内容

```
┌─────────────────────────────────┐
│  78%              使用电池       │
│  ─────────────────────────────  │
│  当前功耗          11.2 W        │
│  每 1% 约          4分28秒       │
│  预计剩余          5小时42分     │
│  ─────────────────────────────  │
│  最近实测                        │
│    79 → 78%       6分12秒        │
│    80 → 79%       6分29秒        │
│    81 → 80%       4分03秒        │
│  ─────────────────────────────  │
│  循环次数          214 次        │
│  健康度            38.4 / 52.0 Wh (74%)
│  温度              32.5 °C       │
│  出厂日期          2022-08       │
└─────────────────────────────────┘
```

「循环次数」「温度」「出厂日期」这三行**在拿不到值的机器上整行不出现**，面板相应变矮；
只有「健康度」是几乎一定拿得到的（设计容量和满充容量属于必填字段）。详见下面的降级规则。

### 数据来源

大头是现成的。`src/battery_power.cpp` 已经打开设备、拿到 tag、查了 `BatteryInformation`
和 `BATTERY_STATUS`，下面标「已在查」的字段现在就在手里，只是没往上传：

| 显示项 | 来源 | 可用性 |
| --- | --- | --- |
| 当前功耗 / 每 1% / 剩余 | `BATTERY_STATUS.Rate` / `.Capacity` + `FullChargedCapacity` | 已在查，已验证可用 |
| **循环次数** | `BATTERY_INFORMATION.CycleCount` | 已在查，**很多机器报 0** —— 见下 |
| 设计容量 | `BATTERY_INFORMATION.DesignedCapacity`（mWh） | 已在查 |
| 当前满充容量 | `BATTERY_INFORMATION.FullChargedCapacity`（mWh） | 已在查 |
| 健康度 | 上面两个相除 | 同上 |
| 电压 | `BATTERY_STATUS.Voltage`（mV） | 已在查 |
| 化学类型 | `BATTERY_INFORMATION.Chemistry[4]`，如 `LION` / `LiP` | 已在查，**非 null 结尾**，按定长 4 字节读 |
| 出厂日期 | `IOCTL_BATTERY_QUERY_INFORMATION` + `BatteryManufactureDate` | 需新加一次查询，部分驱动不支持 |
| **温度** | 同上 + `BatteryTemperature` | 需新加，**多数消费级笔记本不支持** —— 见下 |
| 厂商 / 型号 / 序列号 | 同上 + `BatteryManufactureName` / `BatteryDeviceName` / `BatterySerialNumber` | 需新加，返回宽字符串 |

**循环次数报 0 是常态，不是 bug**：`CycleCount` 由 ACPI 的 `_BIF`/`_BIX` 方法提供，
很多 OEM 的固件干脆不填。`powercfg /batteryreport` 那一栏空着的机器，这里也一定是 0 ——
它读的是同一个地方，没有第二条路（WMI 的 `root\WMI` 里那个类同样转发自这个驱动）。

#### 电池温度

沿用现成的查询辅助函数，换一个 `InformationLevel` 即可：

```cpp
ULONG tenths_kelvin = 0; // BatteryTemperature reports 0.1 K units
if (query_information(device, tag, BatteryTemperature, &tenths_kelvin, sizeof(tenths_kelvin))) {
    const double celsius = tenths_kelvin / 10.0 - 273.15;
}
```

- **不支持时的表现和循环次数不一样**：`CycleCount` 是调用成功、值为 0；温度是
  `DeviceIoControl` **直接失败**（`GetLastError` 给 `ERROR_INVALID_FUNCTION` 或
  `ERROR_NOT_SUPPORTED`）。所以温度必须靠返回值判断，不能靠检查数值。
- 拿到值也要做**合理性钳制**：见过驱动把没填的字段返回成 0 K 或 2731（正好 0.00 °C）的。
  超出 0–80 °C 一律当不可用 —— 这个区间之外要么是驱动在瞎报，要么电池已经出事了，
  哪种都不该显示给用户看。
- **温度是动态字段，要跟功耗一起每秒重采**；循环次数、设计容量、出厂日期是静态的，
  打开面板时读一次就够。别把这两类混在一个刷新路径里。
- **不要拿 `MSAcpi_ThermalZoneTemperature` 顶替**：那是 CPU / 主板热区，不是电池温度，
  数值差得远，而且要走 WMI。这条路走不通，省得以后再试一次。

#### 不可用的字段一律不显示

**不显示「不可用」「N/A」「--」，直接整行不画。** 循环次数、温度、出厂日期这几项在不少机器上
永远拿不到，画一行占位符等于每次打开面板都提醒用户一次他的固件不给数，纯噪音。

- 一整段的字段全都拿不到时，**连同那段的小标题和分隔线一起省掉**，别留一条孤零零的横线。
- 因此**面板高度是动态的**：得先在布局阶段算出实际要画多少行，再据此定窗口大小和弹出位置，
  不能把高度写死在窗口创建时。这是隐藏行带来的真实约束，实现时先把这一步想清楚。

### 「最近实测」的攒法

- 内存里一个定长环形缓冲（十几项足够），每次电量百分比变化时压一条：时间戳、变化前后的百分比、方向。
  **不落盘** —— 便携性要求见规格 1.5，而且这本来就是「本次开机以来」的观察窗口。
- **跨充放电切换要断开**：`80→79`（放电）和 `79→80`（充电）不能混在一个列表里。切换时清空或分段。
- **睡眠期间的间隔要作废**：合盖睡 8 小时唤醒后掉了 3%，那一格的「时长」是 8 小时，除出来毫无意义。
  用 `PBT_APMSUSPEND` / `PBT_APMRESUMEAUTOMATIC` 打标记，跨过标记的那一格直接丢弃。
  （`QueryUnbiasedInterruptTime` 与挂钟对比也能识别，但既然消息已经在收，用消息更直接。）
- 刚开机时这个列表是空的，要有「还没有数据」的占位。这是这块功能的固有短板，接受。

### 窗口与交互

- **触发**：托盘图标左键**双击**（`NIN_KEYSELECT` / `WM_LBUTTONDBLCLK`，目前完全没绑定）。
  单击不要触发 —— 单击到托盘图标是最容易误触的操作。
- **形态**：`WS_POPUP | WS_EX_TOOLWINDOW`，无标题栏，圆角边框自绘。不进任务栏、不进 Alt+Tab。
- **定位**：以托盘图标为锚点弹出，跟 Windows 自己的音量 / 网络 / 电量面板同一种行为——
  紧贴任务栏，从图标那一侧升起。任务栏在下方时是这样：

  ```
  ┌──────────────────────────────────┐
  │                                  │
  │  工作区              ┌─────────┐ │
  │                      │  面板   │ │
  │                      └─────────┘ │ ← 面板底边贴工作区底边
  ├──────────────────────────┬───────┤
  │ 任务栏               [78]│ ^ ♪ ⌨ │ ← 锚点 = 托盘图标矩形
  └──────────────────────────┴───────┘
                             ↑ 面板右边缘与图标右边缘对齐，
                               越界则整体往工作区内推
  ```

  两个轴上的规则分开写，四种任务栏位置就统一了：

  - **垂直于任务栏的轴**：面板紧贴工作区在那一侧的边界（任务栏在下 → 面板底边贴工作区底边，
    任务栏在左 → 面板左边贴工作区左边），留几像素间隙。
  - **平行于任务栏的轴**：面板与图标对齐（上图是右边缘对齐），然后钳制进工作区——
    图标离屏幕边缘太近时，面板整体往里推，不允许有任何一部分落在工作区外。

  矩形从 `Shell_NotifyIconGetRect` 拿，工作区从 `MonitorFromRect` + `GetMonitorInfo` 的
  `rcWork` 拿。**不要去猜任务栏在哪一边**：任务栏可以停在四条边中的任意一条，还可以自动隐藏，
  而 `rcWork` 已经把这些都算进去了——比较图标矩形和 `rcWork` 的相对位置就能推出任务栏方位。

  - **先算高度再定位**：面板高度是动态的（见上面的降级规则），高度没定下来就没法确定顶边位置。
  - **图标被折叠进「隐藏的图标」溢出区时**（任务栏上那个 `^` 按钮），`Shell_NotifyIconGetRect`
    返回的是**溢出按钮**的矩形并给出 `S_FALSE`，不是图标的真实位置。这时贴着那个按钮弹出即可，
    但要记得判 `S_FALSE` 而不是只判 `SUCCEEDED`——两者都算调用成功，漏判会把按钮位置当成图标位置。
  - 整个调用失败时（罕见）退回到工作区的右下角，别让面板跑到屏幕外面去。
- **失焦即隐藏**：`WM_ACTIVATE` 收到 `WA_INACTIVE` 就 `SW_HIDE`，跟 Windows 自己的电量面板一致。
  只隐藏不销毁，反复开关不重建窗口。
- **DPI**：面板要处理 `WM_DPICHANGED`，重算布局与字体。托盘图标那套 92% / 超采样的常量与这里无关。
- **字体用 `lfMessageFont`（`SystemParametersInfoForDpi` + `SPI_GETNONCLIENTMETRICS`），不要沿用
  Segoe UI**。规格 2.3 固定 Segoe UI 的理由只针对托盘那种小 ppem 下的内嵌点阵问题，
  面板是正常字号、还有中文文案，系统 UI 字体（中文 Windows 上是微软雅黑）才是对的选择。
- **配色**：需要背景色、文字色、次要文字色、分隔线色各两套（深/浅）。
  沿用规格 2.4 的「启动时读一次、运行期不跟随」，与托盘图标保持一致。

### 刷新与「禁止轮询」的关系

面板**可见时** `SetTimer` 重采功耗与温度，隐藏时立刻 `KillTimer`。

这不违反规格 2.2 —— 那条禁的是空闲态的周期性占用。面板开着意味着用户正在盯着看，
此时定期采样是他要的；面板一关就回到完全事件驱动的状态，稳态行为不变。
**实现时务必确认 `KillTimer` 在所有隐藏路径上都走到**（失焦隐藏、Esc、再次双击）。

**间隔取 2 秒，不要 1 秒。** 数据源本身就不到 1 Hz：`IOCTL_BATTERY_QUERY_STATUS` 底下是 ACPI
的 `_BST`，要跑 AML 解释器再去问 EC，而 EC 内部的采样周期通常是 1–5 秒。每秒查一次多半是把
同一个值重复读四遍，而这次读取并不免费（AML 执行 + EC 通信在某些机器上是毫秒级的）。

配套的两点：

- **面板可见期间保持设备句柄**，别每个 tick 都 `CreateFile` / `CloseHandle`。
  代价是电池热插拔后 tag 会失效，所以 IOCTL 一失败就丢掉句柄重开，别假设它一直有效。
- **静态字段不进 tick**：循环次数、设计容量、出厂日期、厂商型号在面板打开时读一次就够，
  tick 里只重采 `Rate` / `Capacity` / 温度。

另外，瞬时功率抖动很大，2 秒跳一个数看着会乱。**显示值可以做最近 3 次的滑动平均**——
数字稳下来，才好用来对比「开不开某个软件差多少瓦」，那本来就是想停留观察的人要的东西。
（tooltip 那边不需要，它只显示一瞬间，没有连续性可言。）

### 绘制

纯 GDI 手绘一个 `WM_PAINT`，**不用任何控件**。整个面板就是若干行「左标签 + 右数值」，
用 `DrawTextW` 分两次对齐画即可，不值得为它引入控件、布局器或 GDI+。
估计 150–250 行，exe 增加十几 KB。

### 做的时候要一并改的规格

- **2.1 进程形态** —— 现在写的是「无主窗口的托盘常驻程序 / 这个窗口对用户完全不可见」，
  面板会打破这个描述，要改成「一个隐藏的消息窗口 + 一个按需显示的面板窗口」。
- **2.2 刷新机制** —— 补上面板可见时的定时刷新，并说明它为什么不算轮询。
- **2.7 右键上下文菜单** —— 如果决定在菜单里也加一个「电池信息」入口，这节要加一项。
- 新增一节描述面板本身（内容、定位、失焦隐藏、数据来源与不可用时的降级）。
- **1.5 绿色便携** —— 确认一句「面板的历史数据只在内存，不落盘」。

### 分批做的切法

如果不想一次做完，按这个顺序切，每一刀都是可用的：

1. **面板骨架 + 上半部分**（百分比 / 功耗 / 每 1% / 剩余）—— 数据全是现成的，
   等于把 tooltip 的三行搬进一个能停留、能每秒刷新的窗口。
2. **加电池健康那一段**（循环次数 / 健康度 / 化学类型）—— 仍然只用已经在查的
   `BATTERY_INFORMATION`，不新增 IOCTL。整行隐藏的降级规则在这一刀里落地。
3. **加温度** —— 一次新查询，但它是动态字段，要接进每秒刷新那条路径，所以单独一刀。
4. **加「最近实测」** —— 这块最麻烦（环形缓冲 + 充放电分段 + 睡眠作废），单独做。
5. **加剩下的静态字段**（出厂日期 / 厂商型号 / 序列号）—— 收益最小，最后做，不做也行。

### 待确认

- `BatteryTemperature` / `BatteryManufactureDate` 在实机上到底给不给值。
  **动手前先跑 `powercfg /batteryreport` 对一遍**：那份报告里空着的字段，代码里也一定拿不到，
  不值得为它写查询和布局。
- 面板宽度按最长的中文标签定死，还是按内容量出来？定死更简单，但换 DPI 时要重算。
  注意这跟「面板高度是动态的」不冲突——高度必须动态，宽度可以定死。
